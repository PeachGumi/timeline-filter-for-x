# プライバシーポリシー

最終更新: 2026-09-13

Timeline Filter for Xは、X / Twitter上の投稿を利用者が選んだ条件でローカルに非表示にするブラウザ拡張です。

## 取り扱う情報

拡張は`x.com`および`twitter.com`上で、次の情報をフィルター判定に使います。

- 投稿者のユーザー名と認証状態
- 投稿ID
- Xが付与したAI生成ラベル
- Xが返した投稿言語コード
- 言語情報がない場合の投稿本文
- Xが画面へ表示する翻訳元言語ラベル

`inject.js`はページのMAIN worldで`fetch`と`XMLHttpRequest`を監視します。解析するのは、X/TwitterのHTTPSホスト上にある`/graphql/`、`/api/`、または公式APIホスト上の`/1.1/`・`/2/`レスポンスだけです。レスポンス全体を保存せず、フィルターに必要な分類結果だけをメモリ上で`content.js`へ渡します。

MAIN worldと`content.js`の間では、Xページ内の`window`メッセージを使って分類結果を渡します。このメッセージは同じXページ上ですでに動作しているスクリプトから観測・送信できるため、認証情報としては扱いません。受信側は形式を検証し、分類結果は表示のフィルター以外には使いません。元になるXのレスポンスも、もともと同じページが受信したものです。

## 保存する情報

3つのフィルター設定を`chrome.storage.sync`へ保存します。ブラウザの同期機能が有効な場合、設定はブラウザ提供元の同期基盤を通じて利用者の端末間で同期されることがあります。保存期間と同期データの管理はブラウザの設定に従います。

現在のタブで非表示にした件数はメモリ上で数え、ポップアップを開いたときに表示します。件数は永続保存しません。

## 外部送信と共有

本拡張は、開発者や第三者が運営するサーバーへ投稿本文、ユーザー名、閲覧履歴、設定、分類結果を送信しません。解析、広告、テレメトリー、データ販売、第三者共有はありません。リモートコードも読み込みません。

言語判定にはChromiumの`chrome.i18n.detectLanguage`を使います。この処理はブラウザ内で行われ、外部の言語判定サービスは使いません。

## 権限

- `storage`: フィルター設定の保存
- `https://x.com/*`: X上での分類と表示制御
- `https://twitter.com/*`: 旧Twitter URLでの分類と表示制御

Cookie、ブラウザ履歴、ダウンロード、クリップボード、位置情報、すべてのWebサイトへのアクセス権限は要求しません。

## 削除

拡張をアンインストールすると、拡張がローカルに保持するデータはブラウザによって削除されます。同期設定はブラウザの同期仕様に従うため、必要に応じてブラウザ側の同期データも管理してください。

## 変更

データ処理が変わる場合は、この文書と拡張のバージョンを更新します。変更履歴はGitリポジトリで確認できます。

## 問い合わせ

一般的な質問はGitHub Issues、脆弱性や公開すべきでない内容はGitHubのPrivate vulnerability reportingから連絡してください。
